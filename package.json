{
  "name": "localonly-webrtc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "serve": "http-server dist -p 3000 -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "rimraf": "^6.1.3",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
